/*! @brief 消息处理器路由对象
 *
 * 路由对象根据设定的规则匹配消息，将消息传递给符合规则的第一个处理器。后加入的路由规则优先匹配。
 */
class Routing: Handler
{
	/*! @brief 添加一组路由规则
	 * @param map 路由参数
	 */
	append(Object map);

	/*! @brief 添加一条路由规则
	 * @param pattern 消息匹配格式
	 * @param hdlr 消息处理器
	 */
	append(String pattern, Handler hdlr);

	/*! @brief 添加一条 javascript 处理函数路由规则
	 * @param pattern 消息匹配格式
	 * @param hdlr javascript 消息处理器，详见 mq.jsHandler
	 */
	append(String pattern, Value hdlr);
};
