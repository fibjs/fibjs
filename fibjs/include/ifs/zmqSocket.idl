
/*! @brief 网络套接口对象
 */
class zmqSocket : Trigger
{
	zmqSocket(Integer type = zmq.PAIR);
	bind(String addr);
	connect(String addr);
	Buffer recv() async;
	send(Buffer data);
	close();
	readonly Integer type;
};
