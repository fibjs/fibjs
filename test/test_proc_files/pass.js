/*
 * pass.js — scene: normal passing tests
 *
 * Expects: all tests pass, the process exits normally with code 0.
 * (process-level verification lives in test_proc_test.js)
 */
var { describe, it } = require('node:test');

describe('pass', () => {
    it('a', () => {});
    it('b', () => {});
});
